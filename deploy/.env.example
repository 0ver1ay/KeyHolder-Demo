# Copy to deploy/.env for local demo. Change the password before any real deploy.
# cp deploy/.env.example deploy/.env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
# 5433 — avoid conflict with a system PostgreSQL on 5432
DB_PORT=5433
